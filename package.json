{
  "name": "evmfs-browser-extension",
  "version": "1.0.0",
  "private": true,
  "description": "Chrome extension (MV3) — resolve evmfs:// URLs from on-chain EVM storage",
  "scripts": {
    "build": "bash build.sh",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "@vitest/coverage-v8": "^4.1.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.0"
  },
  "license": "MIT"
}
